<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.template.restapi.aggregate.record_category.store.repository.RecordCategoryRepository">

    <!-- 지출 카테고리 -->
    <select id="selectSpending" resultType="com.template.restapi.aggregate.record_category.domain.entity.RecordCategoryDto">
        SELECT rc.record_category_id, rc.record_category_type,
                c.category_id, c.category_name
        FROM record_category rc
        LEFT JOIN category c ON c.category_id BETWEEN 1 AND 18
        WHERE rc.record_category_id = '1'
    </select>

    <!-- 수입 카테고리 -->
    <select id="selectIncome" resultType="com.template.restapi.aggregate.record_category.domain.entity.RecordCategoryDto">
        SELECT rc.record_category_id, rc.record_category_type,
                c.category_id, c.category_name
        FROM record_category rc
        LEFT JOIN category c ON c.category_id BETWEEN 18 AND 23
        WHERE rc.record_category_id = '2'
    </select>

</mapper>
