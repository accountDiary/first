<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.template.restapi.aggregate.record.store.repository.RecordRepository">

    <!-- 가계부 조회 -->
    <select id="selectRecord" parameterType="map" resultType="RecordDto">
        SELECT r.record_id,
                r.record_type,
                r.user_id,
                r.payment_id,
                r.category_id,
                r.record_amount,
                r.record_details,
                r.record_memo,
                r.record_date,
                p.payment_type,
                rc.record_category_type,
                c.category_name
        FROM record r
        LEFT JOIN payment p
        ON r.payment_id = p.payment_id
        LEFT JOIN record_category rc
        ON r.record_type = rc.record_category_id
        LEFT JOIN category c
        ON r.category_id = c.category_id
        WHERE r.user_id = 1
        AND r.record_date = '2024-07-11'
    </select>

    <!-- 가계부 한 줄 추가 -->
    <insert id="insertRecord" parameterType="RecordDto">
        INSERT INTO
            record(record_type, user_id, payment_id, category_id,
                    record_amount, record_details, record_date)
        VALUES (#{record_type}, 1, #{payment_id}, #{category_id},
                    #{record_amount}, #{record_details}, #{record_date})
    </insert>
</mapper>